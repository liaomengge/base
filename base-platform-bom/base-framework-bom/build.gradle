version = '0.0.6-x-SNAPSHOT'

def versions = [
        baseProcessor           : '0.0.1-SNAPSHOT',
        baseCommonUtils         : '1.1.4-x-SNAPSHOT',
        baseCommonMiddleware    : '1.1.4-x-SNAPSHOT',
        baseCommonDayu          : '1.1.4-x-SNAPSHOT',
        baseCommonCache         : '1.1.4-x-SNAPSHOT',
        baseDubboFramework      : '1.1.0-SNAPSHOT',
        baseSpringBootStarter   : '2.x.0-SNAPSHOT',
        baseSpringCloudFramework: '2.x.0-SNAPSHOT'
]

dependencyManagement {
    imports {
        mavenBom "com.github.liaomengge:base-platform-bom:${rootProject.version}"
    }
    dependencies {
        dependency "com.github.liaomengge:ly-async-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-base-mybatis-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-cache-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-dayu-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-endpoint-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-framework-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-fastjson-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-health-check-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-graceful-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-logger-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-mail-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-activemq-spring-boot-starter:1.5.x.58-test003-SNAPSHOT"
        dependency "com.github.liaomengge:ly-metric-druid-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-hikari-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-httpclient-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-redis-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-retrofit-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-metric-undertow-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-mongo-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-mq-lock-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-mq-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-mybatis-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-multi-shardingsphere-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-multi-mybatis-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-quartz-lock-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-quartz-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-redis-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-rest-template-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-retrofit-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-swagger-spring-boot-starter:${versions.baseSpringBootStarter}"
        dependency "com.github.liaomengge:ly-thread-pool-spring-boot-starter:${versions.baseSpringBootStarter}"

        dependency "com.github.liaomengge:base-dubbo-framework:${versions.baseDubboFramework}"

        dependency "com.github.liaomengge:base-spring-cloud-framework:${versions.baseSpringCloudFramework}"

        dependency "com.github.liaomengge:base-common-dayu:${versions.baseCommonDayu}"

        dependency "com.github.liaomengge:base-common-cache:${versions.baseCommonCache}"

        dependency "com.github.liaomengge:base-common-middleware:${versions.baseCommonMiddleware}"

        dependency "com.github.liaomengge:base-common-utils:${versions.baseCommonUtils}"

        dependency "com.github.liaomengge:base-processor:${versions.baseProcessor}"
    }
    resolutionStrategy {
        cacheChangingModulesFor 0, 'seconds'
        cacheDynamicVersionsFor 0, 'seconds'
    }
}