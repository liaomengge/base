def artifactId = "base_dubbo_framework"

dependencies {
    api(project(":service_api"))
    api(project(":service_common"))
    api(project(":data_mapper"))
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: nexusUrl) {
                authentication(userName: "9now", password: "9now123")
            }
            pom.version = "$project.version"
            pom.artifactId = "$artifactId"
            pom.groupId = "$project.group"
        }
    }
}
